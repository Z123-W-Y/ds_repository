按键选题：
	PB11使能失能 
	PB10选题（0~5）
	PB1确认选题，跳出循环
如果按键选择5
	判断接收的是误差数据还是角坐标（并显示）
	PB11使能失能
	PB10 按一次，单片机向openmv发送一次存储坐标信号，并返回给单片机角的序号
	PB1确认四角坐标（可自选几个角），跳出循环
	存储完之后，再次进行选题（PB10选题之后，PB1确认选题跳出循环）
OLED初始化显示当前题数及误差信息
PID初始化（初始化各个选题的PID参数）
确定好以上之后进入while循环
	向openmv发送当前选题数
	PB11使能失能，PB0暂停
	单片机如果接收到一次，进行数据处理（接收到的是1误差数据 ，还是2角坐标数据）
		误差数据：
			如果XY的误差值=1000，表示未识别到（no_shibie=1反之），并显示接收到的误差数据
			系统总开关默认为1

			                                                                                                                                                                              